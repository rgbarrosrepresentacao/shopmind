import CaixaPageClient from '@/components/caixa/caixa-page-client';

export const metadata = {
  title: 'ShopMind — Controle de Caixa',
  description: 'Fechamento, sangria, suprimento e auditoria financeira inteligente com IA Gerente.',
};

export default function CaixaPage() {
  return <CaixaPageClient />;
}
